# Minimal build context: Dockerfile copies only pyproject.toml and docker/entrypoint.sh.
# Runtime trees are bind-mounted (docker-compose.yml).

.git
.venv
.cursor
.worktrees

__pycache__
**/__pycache__
*.pyc
.pytest_cache
.ruff_cache

# Bind-mounted at runtime — never COPY into the image
app
packs
alembic
alembic.ini
config
data
tests
docs
scripts
sources

# Compose / local artifacts
docker-compose*.yml
*.md
LICENSE
NOTICE
